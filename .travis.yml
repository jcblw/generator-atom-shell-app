language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-atom-shell-app' ]; then cd .. && eval "mv $currentfolder generator-atom-shell-app" && cd generator-atom-shell-app; fi

